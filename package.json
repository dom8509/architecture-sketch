{
  "name": "sysarch",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:icons": "tsx scripts/build-icons.ts",
    "typecheck": "npm run build:icons && tsc -b packages/core && tsc -p packages/core/test",
    "test": "npm run build:icons && vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
