# Icons

Icons are single-color symbols on a 24×24 grid. The theme tints them in the text color of the
category. You only set them in templates: `icon <name>` (or `icon none` to remove an inherited
icon). This page is generated from `library/icons/` on every build.

<!--@include: ../_generated/icons.md-->

Custom icons, the rules for icon files, and why diagrams cannot embed images:
[05 Rendering & Export](/concept/05-rendering-export#icons).
