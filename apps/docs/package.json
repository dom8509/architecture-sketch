{
  "name": "@sysarch/docs",
  "version": "0.1.0",
  "private": true,
  "description": "sysarch documentation (VitePress, GitHub Pages)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "generate": "tsx --tsconfig tsconfig.json scripts/generate.ts",
    "dev": "npm run generate && vitepress dev",
    "build": "npm run generate && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.2.0",
    "markdown-it": "^14.3.2",
    "vitepress": "^1.6.4"
  }
}
